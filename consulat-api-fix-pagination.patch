diff --git a/app/Http/Controllers/API/DemandeAdminController.php b/app/Http/Controllers/API/DemandeAdminController.php
index 855fedb..b85c62d 100644
--- a/app/Http/Controllers/API/DemandeAdminController.php
+++ b/app/Http/Controllers/API/DemandeAdminController.php
@@ -50,11 +50,13 @@ public function index(Request $request)
 
         return response()->json([
             'success' => true,
-            'data' => $demandes->through(fn (Demande $d) => $this->formaterDemande($d))->items(),
-            'meta' => [
-                'total' => $demandes->total(),
-                'page' => $demandes->currentPage(),
-                'dernier_page' => $demandes->lastPage(),
+            'data' => [
+                'items' => $demandes->through(fn (Demande $d) => $this->formaterDemande($d))->items(),
+                'meta' => [
+                    'total' => $demandes->total(),
+                    'current_page' => $demandes->currentPage(),
+                    'last_page' => $demandes->lastPage(),
+                ],
             ],
         ]);
     }
diff --git a/app/Http/Controllers/API/RessortissantAdminController.php b/app/Http/Controllers/API/RessortissantAdminController.php
index 2079e2d..057b7b5 100644
--- a/app/Http/Controllers/API/RessortissantAdminController.php
+++ b/app/Http/Controllers/API/RessortissantAdminController.php
@@ -45,11 +45,13 @@ public function index(Request $request)
 
         return response()->json([
             'success' => true,
-            'data' => $ressortissants->through(fn (Ressortissant $r) => $this->formater($r))->items(),
-            'meta' => [
-                'total' => $ressortissants->total(),
-                'page' => $ressortissants->currentPage(),
-                'dernier_page' => $ressortissants->lastPage(),
+            'data' => [
+                'items' => $ressortissants->through(fn (Ressortissant $r) => $this->formater($r))->items(),
+                'meta' => [
+                    'total' => $ressortissants->total(),
+                    'current_page' => $ressortissants->currentPage(),
+                    'last_page' => $ressortissants->lastPage(),
+                ],
             ],
         ]);
     }
